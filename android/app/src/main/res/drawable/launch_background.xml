<?xml version="1.0" encoding="utf-8"?>
<!-- The splash the iOS app shows: a near-black vertical gradient with the lamp
     resting in the middle of it. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="#232323"
                android:endColor="#000000"
                android:type="linear" />
        </shape>
    </item>

    <!-- 246dp of canvas puts the lamp itself at 175dp, the size Android 12 and
         up draws it at inside its own splash — see `values-v31/styles.xml`. -->
    <item
        android:width="246dp"
        android:height="246dp"
        android:gravity="center">
        <bitmap
            android:gravity="fill"
            android:src="@drawable/launch_icon" />
    </item>
</layer-list>
